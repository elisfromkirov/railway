next tasks

> add infra module core class node

> add execution module interface

> add core components
    > memory_view
    > result

> refactor linux io into current structure

railway/
    transport/
        linux/
            detail/
                completion
                reactor
                reactor_entry
                handle
            address
            async_acceptor
            async_reader
            async_writer
            port
            socket
            transport
